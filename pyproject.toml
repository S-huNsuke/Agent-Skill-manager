[project]
name = "agent-skills-manager"
version = "0.1.0"
description = "Bootstrap metadata for the Agent Skills Manager repository."
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["python/tests"]
